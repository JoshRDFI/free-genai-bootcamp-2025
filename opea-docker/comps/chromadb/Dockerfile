FROM chromadb/chroma:latest

# No need for additional setup as the official image has everything
# The official image exposes port 8000 by default

EXPOSE 8000

# The official image has its own entrypoint, no need to override 